# ZAP rule configuration for the AutoCare Hub API scan.
# 10096 - Timestamp Disclosure - Unix:
# Reviewed as non-sensitive for this MVP because the evidence is the generic
# ErrorResponse timestamp returned on 401 responses. It dões not expose business
# data, credentials, stack traces, SQL, infrastructure metadata or internal IDs.
10096	IGNORE	Timestamp Disclosure - Unix reviewed in standard API error response
